name: Node CI

on: push

jobs:
  build:
    runs-on: windows

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credentials: true

      - name: Install 🔧
        run: git log -- https://github.com/PavithraRajendran99/Spinircle/tree/master/.github/workflows\logcheck.csv
